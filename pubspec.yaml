name: simple_document_scanner
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  edge_detection: ^1.1.1
  permission_handler: ^10.0.0
  path_provider: ^2.0.11
  path: ^1.8.2
  provider: ^6.0.5
  reorderable_grid_view: ^2.2.6
  photo_view: ^0.14.0
  pdf: ^3.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
